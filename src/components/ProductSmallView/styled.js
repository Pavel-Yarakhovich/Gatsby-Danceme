import styled from "styled-components";

export const ImageWrapper = styled.div`
  width: 100%;
  // height: 300px;
  overflow: hidden;
`;

export const Title = styled.h3`
  font-size: 1.2em;
  margin: .5em 0;
  padding: 0 .5em;
  text-align: center;
`

export const Article = styled.span`
  font-size: 0.9em;
  color: purple;
`

export const Container = styled.div`
  height: 100%;
  box-shadow: 0px 5px 6px rgba(0, 0, 0, 0.05), 0px 3px 16px rgba(0, 0, 0, 0.03),
      0px 9px 12px rgba(0, 0, 0, 0.04);
`