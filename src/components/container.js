import React from 'react'

export default ({ children }) => (
  <div style={{ width: "100%", minHeight: "100vh", display: "flex", flexFlow: "column", margin: '0 auto', position: "relative" }}>{children}</div>
)
